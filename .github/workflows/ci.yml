name: ci
on:
  pull_request:
  push:
    branches: [main, master]
  workflow_dispatch:

# Build + kunit fan-out across the kernels we support. Each leg is
# pinned to a specific tag so per-PR CI is deterministic — kernel.org
# pushing a new point release does not silently change what a
# contributor's PR builds against.
#
# The pins themselves live in .github/kernel-pins/ (one file per
# tracks: 6.18.json, 7.0.json — single source of truth,
# shared with vm-smoke.yml and vm-smoke-weekly.yml). One file per
# track means parallel bumper PRs touching different tracks don't
# conflict on each other. The `pins` job below globs that directory
# and emits a matrix-shaped output that build-and-kunit then fans
# across with `fail-fast: false` (so one bad kernel doesn't mask
# the other).
#
# Special case for the bumper: when the PR is on a
# `bot/bump-kernel-pins-<key>` branch, the matrix is filtered to a
# single leg for <key>. The other tracks' refs didn't change on a
# track-specific bump PR, so re-running them is wasted runner time;
# their last green main result is still the relevant signal. The
# vm-smoke check applies the same routing (vm-smoke.yml).
#
# Pins move via .github/workflows/bump-kernel-pins.yml, which runs
# weekly and opens one PR per track with the new value; that PR's
# checks then exercise the new kernel before the bump can land. The
# 6.18 is the baseline support track; the 7.0 leg verifies the
# next stable series against the same 6.18+ source baseline.

jobs:
  # Python trace-tool tests are kernel-ref-independent, so they run
  # once here instead of on every matrix leg of build-and-kunit.
  pytest:
    name: pytest
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Setup harness (apt + pip)
        uses: ./.github/actions/setup-harness
        with:
          install-pip: "true"
      - name: Python unit tests + coverage
        run: ./tools/tests/pytest.sh

  pins:
    name: load kernel pins
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    outputs:
      matrix: ${{ steps.read.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: .github/kernel-pins/
          sparse-checkout-cone-mode: false
      - id: read
        name: Read .github/kernel-pins/
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          set -e
          # On a bumper PR (bot/bump-kernel-pins-<key>), restrict the
          # matrix to that single track. Other tracks' refs didn't
          # change on the bump PR, so their last green main result
          # is still the signal. github.head_ref is empty on push
          # and workflow_dispatch — those fall through to the full
          # matrix.
          ONLY_KEY=""
          case "$HEAD_REF" in
            bot/bump-kernel-pins-*) ONLY_KEY="${HEAD_REF#bot/bump-kernel-pins-}" ;;
          esac
          if [[ -n "$ONLY_KEY" && ! -f ".github/kernel-pins/${ONLY_KEY}.json" ]]; then
            echo "::error::no .github/kernel-pins/${ONLY_KEY}.json (head_ref=$HEAD_REF)" >&2
            exit 1
          fi
          INCLUDE=$(
            for f in .github/kernel-pins/*.json; do
              name="$(basename "$f" .json)"
              if [[ -n "$ONLY_KEY" && "$name" != "$ONLY_KEY" ]]; then
                continue
              fi
              jq --arg name "$name" \
                 '{name: $name, ref: .ref, url: .url}' "$f"
            done | jq -sc 'sort_by(.name)'
          )
          MATRIX=$(jq -nc --argjson inc "$INCLUDE" '{include: $inc}')
          echo "matrix=$MATRIX" >> "$GITHUB_OUTPUT"

  build-and-kunit:
    name: build-and-kunit (${{ matrix.name }})
    needs: pins
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.pins.outputs.matrix) }}
    uses: ./.github/workflows/build-and-kunit.yml
    with:
      linux-url:    ${{ matrix.url }}
      linux-ref:    ${{ matrix.ref }}
      display-name: ${{ matrix.name }}
